#keyとバリュー型の書き方
sudo: required
services: docker
before_install:
  - docker-compose up --build -d
script:
  - docer-compose exec --env 'RAILS_ENV=test' web rails db:create
  - docer-compose exec --env 'RAILS_ENV=test' webrails db:migrate
  - docer-compose exec --env 'RAILS_ENV=test' webrails test

